/** The same quiet handoff for selectors throughout the portfolio. */
export const selectionEase = [.22, 1, .36, 1] as const
export const selectionTransition = { duration: .32, ease: selectionEase }
export const selectionDistance = 8
